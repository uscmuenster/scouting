name: Update scouting vbl

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * *'  # täglich um 04:00 Uhr UTC

permissions:
  contents: write

jobs:
  build-usc-stats-overview:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Check out repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: pip install -r requirements.txt

      - name: 🔍 Sammle VBL-Matchdaten
        env:
          PYTHONPATH: Scouting
        run: python Scouting/scripts/update_vbl_match_data.py --use-default-datasets

      - name: 💾 Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data/vbl/
          git commit -m "chore: update VBL match data" || echo "No changes"
          git push
